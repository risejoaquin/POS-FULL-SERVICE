<UserControl x:Class="PosCore.Views.Controls.CartPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="500"
             Background="#F9FAFB">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Carrito de Compra" FontSize="20" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,10"/>

        
        <!-- Items List -->
        <ListView Grid.Row="1" ItemsSource="{Binding Cart}" BorderThickness="0" Background="White"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Margin="0" Padding="15,10" Background="Transparent" HorizontalAlignment="Stretch">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Desc & Price -->
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="15" Foreground="#111827" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Price, StringFormat=C}" FontSize="13" Foreground="#10B981" FontWeight="Medium" Margin="0,2,0,0"/>
                            </StackPanel>
                            
                            <!-- Quantity Controls -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Button Content="-" Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Width="32" Height="32" Background="#F3F4F6" Foreground="#4B5563" BorderThickness="0" FontWeight="Bold" Cursor="Hand" FontSize="18">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                                </Button>
                                <TextBlock Text="{Binding Quantity}" Width="36" TextAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="16" Foreground="#111827"/>
                                <Button Content="+" Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Width="32" Height="32" Background="#10B981" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand" FontSize="18">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                                </Button>
                            </StackPanel>
                            
                            <!-- Subtotal & Remove -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding SubTotal, StringFormat=C}" FontWeight="Bold" FontSize="16" Foreground="#111827" VerticalAlignment="Center" Width="90" TextAlignment="Right" Margin="0,0,15,0"/>
                                <Button Content="✕" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Width="32" Height="32" Background="#FEE2E2" Foreground="#EF4444" BorderThickness="0" Cursor="Hand" FontWeight="Bold" VerticalAlignment="Center">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="16"/></Style></Button.Resources>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        
        <!-- Footer Flotante -->
        <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,15,0,0">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <TextBlock Text="Subtotal:" FontSize="15" Foreground="#64748B"/>
                    <TextBlock Text="{Binding SubTotal, StringFormat=C}" FontSize="15" Foreground="#475569" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                </Grid>
                <Grid Margin="0,0,0,10">
                    <TextBlock Text="Impuestos:" FontSize="15" Foreground="#64748B"/>
                    <TextBlock Text="$0.00" FontSize="15" Foreground="#475569" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                </Grid>
                <Border BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Margin="0,5,0,15"/>
                <Grid>
                    <TextBlock Text="Total a Pagar:" FontSize="20" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Total, StringFormat=C}" FontSize="32" FontWeight="Black" Foreground="#0d4722" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
