<UserControl x:Class="PosCore.Views.Controls.ProductsPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Categories List -->
        
        <!-- Categories List -->
        <Grid Grid.Row="1" Margin="0,10,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="CATÁLOGO DE PRODUCTOS" FontWeight="Bold" FontSize="11" Foreground="#9CA3AF" Margin="10,0,0,10"/>
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="5,0">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Todos" Background="#6B7280" Foreground="White" Margin="5,0" Padding="15,6" BorderThickness="0" FontWeight="Bold" FontSize="13">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                    </Button>
                    <Button Content="Tacos" Background="White" Foreground="#EF4444" BorderBrush="#EF4444" BorderThickness="1" Margin="5,0" Padding="15,6" FontWeight="Bold" FontSize="13">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                    </Button>
                    <Button Content="Bebidas" Background="White" Foreground="#3B82F6" BorderBrush="#3B82F6" BorderThickness="1" Margin="5,0" Padding="15,6" FontWeight="Bold" FontSize="13">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                    </Button>
                    <Button Content="Postres" Background="White" Foreground="#F59E0B" BorderBrush="#F59E0B" BorderThickness="1" Margin="5,0" Padding="15,6" FontWeight="Bold" FontSize="13">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                    </Button>
                    <Button Content="Snacks" Background="White" Foreground="#8B5CF6" BorderBrush="#8B5CF6" BorderThickness="1" Margin="5,0" Padding="15,6" FontWeight="Bold" FontSize="13">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                    </Button>
                    <Button Content="Extras" Background="White" Foreground="#10B981" BorderBrush="#10B981" BorderThickness="1" Margin="5,0" Padding="15,6" FontWeight="Bold" FontSize="13">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Grid>


        
        <!-- Products List -->
        <ListView Grid.Row="2" ItemsSource="{Binding FilteredProducts}" BorderThickness="0" Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Margin="0" Padding="15,10" Background="Transparent" HorizontalAlignment="Stretch">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon -->
                            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#F3F4F6" Margin="0,0,15,0">
                                <TextBlock Text="🍽️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="#111827"/>
                                <TextBlock Text="{Binding Barcode}" FontSize="11" Foreground="#9CA3AF" Margin="0,2,0,0"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Column="2" Text="{Binding Price, StringFormat=C}" Foreground="#111827" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="0,0,20,0"/>
                            
                            <Button Grid.Column="3" Content="+" 
                                    Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Width="36" Height="36" 
                                    Background="#10B981" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="20" FontWeight="Medium" VerticalAlignment="Center">
                                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
