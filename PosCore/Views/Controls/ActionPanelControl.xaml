<UserControl x:Class="PosCore.Views.Controls.ActionPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="White">
    <UserControl.Resources>
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="ACCIONES RÁPIDAS" FontWeight="Bold" FontSize="11" Foreground="#9CA3AF" Margin="5,0,0,10"/>
            <Button Command="{Binding OpenShiftCommand}" Background="#EEF2FF" Foreground="#4F46E5" Margin="0,0,0,10" Height="50" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></Button.Resources>
                <Grid Width="250">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Caja" Grid.Column="0" FontFamily="Segoe UI Emoji" FontSize="18" VerticalAlignment="Center" Margin="10,0"/>
                    <TextBlock Text="Apertura / Corte de Caja (F4)" Grid.Column="1" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                </Grid>
            </Button>
            <Button Command="{Binding SuspendOrderCommand}" Background="#FEF3C7" Foreground="#D97706" Margin="0,0,0,10" Height="50" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></Button.Resources>
                <Grid Width="250">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="⏸️" Grid.Column="0" FontFamily="Segoe UI Emoji" FontSize="18" VerticalAlignment="Center" Margin="10,0"/>
                    <TextBlock Text="Suspender Venta (F2)" Grid.Column="1" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                </Grid>
            </Button>
            <Button Command="{Binding ResumeOrderCommand}" Background="#ECFDF5" Foreground="#059669" Margin="0,0,0,10" Height="50" BorderThickness="0" Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></Button.Resources>
                <Grid Width="250">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="▶️" Grid.Column="0" FontFamily="Segoe UI Emoji" FontSize="18" VerticalAlignment="Center" Margin="10,0"/>
                    <TextBlock Text="Recuperar Venta (F3)" Grid.Column="1" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                </Grid>
            </Button>
        </StackPanel>


        
        <!-- Shortcuts 3x3 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="ATAJOS" FontWeight="Bold" FontSize="11" Foreground="#9CA3AF" Margin="5,0,0,5"/>
            <ItemsControl ItemsSource="{Binding Shortcuts}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" Rows="3" Margin="0"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.ExecuteShortcutCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding Action}"
                                Background="{Binding Color}"
                                Foreground="White" Margin="4" BorderThickness="0" Cursor="Hand" Height="70" ToolTip="{Binding Description}">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></Button.Resources>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Icon}" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,2"/>
                                <TextBlock Text="{Binding Name}" FontSize="10" FontWeight="Bold" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>


        <!-- Espaciador -->
        <Border Grid.Row="2"/>

        
        <!-- Cobrar -->
        <Button Grid.Row="3" Command="{Binding CheckoutCommand}" Background="#10B981" Foreground="White" Height="70" BorderThickness="0" Cursor="Hand" Margin="0,15,0,0">
            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="12"/></Style></Button.Resources>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="🛒" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="COBRAR VENTA" FontWeight="Black" FontSize="20" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

    </Grid>
</UserControl>
