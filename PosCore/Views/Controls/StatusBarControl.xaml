<UserControl x:Class="PosCore.Views.Controls.StatusBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800"
             Background="{StaticResource PrimaryBrush}">
        <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="15,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" Text="{Binding Settings.WhiteLabel.CompanyName, StringFormat='{}☕ {0}'}" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
        
        <!-- Toast Notification Overlay inside StatusBar instead of popping up over everything, or we can keep it here if we want -->
        <TextBlock Grid.Column="1" Text="{Binding NotificationMessage}" Foreground="#FCD34D" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding IsNotificationVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
            <!-- Módulos de Administrador / Gerente -->
            <Button Command="{Binding OpenInventoryCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Margin="0,0,10,0" ToolTip="Inventario (F8)" Cursor="Hand" FontSize="14" FontWeight="Bold" Padding="10,4" Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📦" Margin="0,0,5,0"/>
                    <TextBlock Text="Inventario"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding OpenUsersCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Margin="0,0,10,0" ToolTip="Usuarios (F7)" Cursor="Hand" FontSize="14" FontWeight="Bold" Padding="10,4" Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="👥" Margin="0,0,5,0"/>
                    <TextBlock Text="Usuarios"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding OpenReportsCommand}" Background="#4F46E5" Foreground="White" BorderThickness="0" Margin="0,0,10,0" ToolTip="Reportes (F6)" Cursor="Hand" FontSize="14" FontWeight="Bold" Padding="10,4" Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊" Margin="0,0,5,0"/>
                    <TextBlock Text="Reportes"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding OpenReturnsCommand}" Background="{StaticResource ErrorBrush}" Foreground="White" BorderThickness="0" Margin="0,0,15,0" ToolTip="Devoluciones (F5)" Cursor="Hand" FontSize="14" FontWeight="Bold" Padding="10,4">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="↩️" Margin="0,0,5,0"/>
                    <TextBlock Text="Devoluciones"/>
                </StackPanel>
            </Button>

            <!-- Herramientas Base -->
            <Button Content="⚙️" Command="{Binding OpenSettingsCommand}" MinWidth="30" Padding="15,0" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" Margin="0,0,10,0" ToolTip="Atajos y Configuración" Cursor="Hand" FontSize="16"/>
            <Button Content="🚪" Command="{Binding LogoutCommand}" MinWidth="30" Padding="15,0" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" Margin="0,0,10,0" ToolTip="Cerrar sesión" Cursor="Hand" FontSize="16"/>
            <Button Content="🖨️" Command="{Binding ConfigurePrinterCommand}" MinWidth="30" Padding="15,0" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" Margin="0,0,15,0" ToolTip="Configurar Impresora" Cursor="Hand" FontSize="16"/>
            <Ellipse Width="12" Height="12" Fill="{Binding SyncStatusColor}" Margin="0,0,8,0" />
            <TextBlock Text="{Binding SyncStatusMessage}" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
