<Window x:Class="PosCore.Views.ProductModifiersConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Modificadores" SizeToContent="WidthAndHeight" MinWidth="600"
        WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Grupos de Modificadores Disponibles" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <GroupBox Header="Modificadores Globales" Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold">
                <ListBox x:Name="AllModifiersList" DisplayMemberPath="Name" Height="250" BorderThickness="0" Margin="5" Background="Transparent"/>
            </GroupBox>
            
            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Content="→" Width="40" Height="40" Margin="0,0,0,10" Click="BtnAddModifier_Click" Background="#E0F2FE" Foreground="#0369A1" BorderBrush="#BAE6FD" BorderThickness="1" Cursor="Hand" FontWeight="Bold" FontSize="18"/>
                <Button Content="←" Width="40" Height="40" Click="BtnRemoveModifier_Click" Background="#FEF2F2" Foreground="#991B1B" BorderBrush="#FECACA" BorderThickness="1" Cursor="Hand" FontWeight="Bold" FontSize="18"/>
            </StackPanel>
            
            <GroupBox Header="Modificadores del Producto" Grid.Column="2" BorderBrush="{StaticResource BorderBrush}" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold">
                <ListBox x:Name="ProductModifiersList" DisplayMemberPath="Name" Height="250" BorderThickness="0" Margin="5" Background="Transparent"/>
            </GroupBox>
        </Grid>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" MinWidth="100" Padding="15,0" Height="40" Background="{StaticResource BorderBrush}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="BtnCancel_Click"/>
            <Button Content="Guardar" MinWidth="120" Padding="15,0" Height="40" Background="{StaticResource SuccessBrush}" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
