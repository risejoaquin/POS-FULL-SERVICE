<Window x:Class="PosCore.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reportes Avanzados" MinHeight="750" SizeToContent="WidthAndHeight" MinWidth="1000" WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header & Filters -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Reportes Avanzados" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Hoy" Command="{Binding FilterTodayCommand}" Padding="10,5" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Cursor="Hand" Margin="0,0,5,0">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
                <Button Content="Última Sem." Command="{Binding FilterLastWeekCommand}" Padding="10,5" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Cursor="Hand" Margin="0,0,5,0">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
                <Button Content="Este Mes" Command="{Binding FilterThisMonthCommand}" Padding="10,5" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Cursor="Hand" Margin="0,0,15,0">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
                
                <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                <DatePicker SelectedDate="{Binding StartDate}" Width="105" Margin="0,0,10,0" Background="{StaticResource SurfaceBrush}"/>
                
                <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                <DatePicker SelectedDate="{Binding EndDate}" Width="105" Margin="0,0,10,0" Background="{StaticResource SurfaceBrush}"/>
                
                <Button Content="🔍" Command="{Binding LoadDataCommand}" ToolTip="Filtrar" MinWidth="40" Padding="10,0" Background="{StaticResource SuccessBrush}" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
                <Button Content="Excel" Command="{Binding ExportToCsvCommand}" MinWidth="60" Padding="15,0" Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
                <Button Content="PDF" Command="{Binding ExportEndOfDayPdfCommand}" MinWidth="60" Padding="15,0" Background="{StaticResource ErrorBrush}" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
        
        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Ingresos del Periodo" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding PeriodTotalRevenue, StringFormat={}{0:C}}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Órdenes del Periodo" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding PeriodTotalOrders}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Tabs for Details -->
        <TabControl Grid.Row="3" Background="Transparent" BorderThickness="0" Margin="-4,0,-4,0" TabStripPlacement="Left">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="20,15"/>
                    <Setter Property="Margin" Value="0,0,0,5"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter ContentSource="Header" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#F1F5F9"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="False"/>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Background" Value="#F8FAFC"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    </MultiTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            
            <TabItem Header="Ventas por Día" FontSize="16">
                <Grid Margin="4,10,4,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20,20,20,10" Margin="0,0,0,15">
                        <Grid Height="180">
                            <!-- Reference lines -->
                            <UniformGrid Rows="4" Columns="1">
                                <Border BorderBrush="#F1F5F9" BorderThickness="0,1,0,0"/>
                                <Border BorderBrush="#F1F5F9" BorderThickness="0,1,0,0"/>
                                <Border BorderBrush="#F1F5F9" BorderThickness="0,1,0,0"/>
                                <Border BorderBrush="#F1F5F9" BorderThickness="0,1,0,1"/>
                            </UniformGrid>
                            
                            <!-- Bars -->
                            <ItemsControl ItemsSource="{Binding DailySales}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="1" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="5,0" VerticalAlignment="Bottom" ToolTip="{Binding TotalRevenue, StringFormat=C}">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <!-- Gradient-like modern bar -->
                                            <Border Width="35" VerticalAlignment="Bottom" Height="{Binding ChartHeight}" CornerRadius="4,4,0,0">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                        <GradientStop Color="#34D399" Offset="0"/>
                                                        <GradientStop Color="#059669" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                            </Border>
                                            
                                            <TextBlock Grid.Row="1" Text="{Binding Date, StringFormat='dd/MM'}" HorizontalAlignment="Center" Margin="0,8,0,0" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding DailySales}" AutoGenerateColumns="False" IsReadOnly="True"
                              Background="{StaticResource SurfaceBrush}" MinRowHeight="40" AlternatingRowBackground="{StaticResource BackgroundBrush}" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" HeadersVisibility="Column" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Date, StringFormat='dd/MM/yyyy'}" Width="*"/>
                            <DataGridTextColumn Header="Órdenes" Binding="{Binding TotalOrders}" Width="*"/>
                            <DataGridTextColumn Header="Ingresos Totales" Binding="{Binding TotalRevenue, StringFormat={}{0:C}}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            
            <TabItem Header="Productos Más Vendidos" FontSize="16">
                <DataGrid ItemsSource="{Binding TopProducts}" AutoGenerateColumns="False" IsReadOnly="True" Margin="4,10,4,0"
                          Background="{StaticResource SurfaceBrush}" MinRowHeight="40" AlternatingRowBackground="{StaticResource BackgroundBrush}" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" HeadersVisibility="Column" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Producto" Binding="{Binding ProductName}" Width="2*"/>
                        <DataGridTextColumn Header="Cantidad Vendida" Binding="{Binding QuantitySold}" Width="*"/>
                        <DataGridTextColumn Header="Ingresos Generados" Binding="{Binding TotalRevenue, StringFormat={}{0:C}}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Métodos de Pago" FontSize="16">
                <DataGrid ItemsSource="{Binding PaymentMethods}" AutoGenerateColumns="False" IsReadOnly="True" Margin="4,10,4,0"
                          Background="{StaticResource SurfaceBrush}" MinRowHeight="40" AlternatingRowBackground="{StaticResource BackgroundBrush}" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" HeadersVisibility="Column" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Método" Binding="{Binding Method}" Width="2*"/>
                        <DataGridTextColumn Header="Transacciones" Binding="{Binding TransactionCount}" Width="*"/>
                        <DataGridTextColumn Header="Ingresos" Binding="{Binding TotalRevenue, StringFormat={}{0:C}}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Historial de Arqueos" FontSize="16">
                <DataGrid ItemsSource="{Binding ShiftHistory}" AutoGenerateColumns="False" IsReadOnly="True" Margin="4,10,4,0"
                          Background="{StaticResource SurfaceBrush}" MinRowHeight="40" AlternatingRowBackground="{StaticResource BackgroundBrush}" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" HeadersVisibility="Column" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Apertura" Binding="{Binding OpenedAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="*"/>
                        <DataGridTextColumn Header="Cierre" Binding="{Binding ClosedAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="*"/>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding OpenedBy}" Width="*"/>
                        <DataGridTextColumn Header="Fondo" Binding="{Binding StartingCash, StringFormat={}{0:C}}" Width="*"/>
                        <DataGridTextColumn Header="Diferencia" Binding="{Binding Difference, StringFormat={}{0:C}}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Movimientos de Caja" FontSize="16">
                <DataGrid ItemsSource="{Binding CashMovements}" AutoGenerateColumns="False" IsReadOnly="True" Margin="4,10,4,0"
                          Background="{StaticResource SurfaceBrush}" MinRowHeight="40" AlternatingRowBackground="{StaticResource BackgroundBrush}" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" HeadersVisibility="Column" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="*"/>
                        <DataGridTextColumn Header="Tipo" Binding="{Binding Type}" Width="*"/>
                        <DataGridTextColumn Header="Monto" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="*"/>
                        <DataGridTextColumn Header="Motivo" Binding="{Binding Reason}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <TabItem Header="Alerta: Bajo Stock (Productos)" FontSize="16">
                <DataGrid ItemsSource="{Binding LowStockProducts}" AutoGenerateColumns="False" IsReadOnly="True" Margin="4,10,4,0"
                          Background="{StaticResource SurfaceBrush}" MinRowHeight="40" AlternatingRowBackground="#FEF2F2" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" HeadersVisibility="Column" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding Barcode}" Width="*"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="2*"/>
                        <DataGridTextColumn Header="Stock Actual" Binding="{Binding StockQuantity}" Width="*"/>
                        <DataGridTextColumn Header="Umbral Min." Binding="{Binding MinStockThreshold}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <TabItem Header="Alerta: Bajo Stock (Insumos)" FontSize="16">
                <DataGrid ItemsSource="{Binding LowStockSupplies}" AutoGenerateColumns="False" IsReadOnly="True" Margin="4,10,4,0"
                          Background="{StaticResource SurfaceBrush}" MinRowHeight="40" AlternatingRowBackground="#FEF2F2" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" HeadersVisibility="Column" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="2*"/>
                        <DataGridTextColumn Header="Stock Actual" Binding="{Binding Stock, StringFormat=N2}" Width="*"/>
                        <DataGridTextColumn Header="Umbral Min." Binding="{Binding MinStockThreshold, StringFormat=N2}" Width="*"/>
                        <DataGridTextColumn Header="Unidad" Binding="{Binding UnitOfMeasure}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
