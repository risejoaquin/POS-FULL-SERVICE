<Window x:Class="PosCore.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuración de Atajos" MinHeight="450" SizeToContent="WidthAndHeight" MinWidth="600" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Perfil de Industria:" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="CmbIndustry" Width="180" FontSize="14" SelectionChanged="CmbIndustry_SelectionChanged" Padding="5">
                <ComboBoxItem Content="Abarrotes" IsSelected="True" />
                <ComboBoxItem Content="Restaurante" />
                <ComboBoxItem Content="Farmacia" />
            </ComboBox>
        </StackPanel>
        
        <DataGrid x:Name="GridShortcuts" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Background="{StaticResource SurfaceBrush}" MinRowHeight="35" FontSize="14" BorderBrush="{StaticResource BorderBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="130" />
                <DataGridTextColumn Header="Ícono" Binding="{Binding Icon, UpdateSourceTrigger=PropertyChanged}" Width="60" />
                <DataGridTextColumn Header="Color (Hex)" Binding="{Binding Color, UpdateSourceTrigger=PropertyChanged}" Width="90" />
                <DataGridComboBoxColumn Header="Acción" SelectedItemBinding="{Binding Action, UpdateSourceTrigger=PropertyChanged}" Width="140">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding AvailableActions, RelativeSource={RelativeSource AncestorType=Window}}" />
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding AvailableActions, RelativeSource={RelativeSource AncestorType=Window}}" />
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancelar" MinWidth="90" Padding="15,0" Height="35" Margin="0,0,10,0" FontSize="14" Click="BtnCancel_Click" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>
            <Button Content="Guardar" MinWidth="90" Padding="15,0" Height="35" Background="{StaticResource PrimaryBrush}" Foreground="White" FontSize="14" FontWeight="Bold" BorderThickness="0" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
