import os
import re

def check_missing_members():
    # we can use reflection if we can compile, but we can't compile.
    # let's write a C# script to compile the project! We have `dotnet`? No.
    # wait! Does the environment have `csc`? 
    pass

