<UserControl x:Class="PosBuilder.Views.Controls.ColorPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300">
    <StackPanel>
        <!-- Hex Input & Preview -->
        <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Background="{StaticResource SurfaceBrush}" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Border x:Name="ColorPreview" Grid.Column="0" Width="20" Height="20" Background="#2563EB" CornerRadius="8" Margin="10,0,5,0" VerticalAlignment="Center"/>
                <TextBox x:Name="HexInput" Grid.Column="1" Padding="5,10" FontSize="14" BorderThickness="0" Background="Transparent" TextChanged="HexInput_TextChanged" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="🎨" Click="OpenColorPicker_Click" Width="40" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Abrir Selector de Color"/>
            </Grid>
        </Border>
        
        <ItemsControl x:Name="PaletteItems">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="8" Rows="1"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Background="{Binding Brush}" Width="28" Height="28" Margin="0,0,6,0" Cursor="Hand" Click="PaletteColor_Click" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>